#include <iostream>
using namespace std;
int main()
{
  int x = 4,y=6;
  bool res =(x>y)&&(x++);
  cout<<res<<"\n";
  cout<<x<<"\n";
  bool a= (y>x)&&(x++);
  cout<<a<<"\n";
  cout<<x;
 return 0;
  
}
