//multiple compound assignment operators illustration

#include <iostream>
using namespace std;
int main()
{
  int x = 4;
  x+=6;
  cout<<x<<"\n";
  x*=5;
  cout<<x<<"\n";
  x%=10;
  cout<<x<<"\n";
  x-=5;
  cout<<x;
  return 0;
  
}
