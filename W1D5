#include <iostream>
using namespace std;

int main() {
    // code here
    int a;
    cout<<"Enter a no."<<"\n";
    cin>>a;
    if(a%2==0)
    cout<<"Even";
    else
    cout<<"Odd";

    return 0;
}
