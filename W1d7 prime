#include <iostream>
using namespace std;

int main() 
{    int n;
cin>>n;
int k=0;
if(n==1)
 cout<<" Neither P nor C";
else if(n==2)
 cout<<"Yes";
else
{for(int i=2;i<n;i++)
  {if(n%i==0)
   k++;}
   if(k>0)
   cout<<"No";
   else
   cout<<"yes";}
   return 0;
}
